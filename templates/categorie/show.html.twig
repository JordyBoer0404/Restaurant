{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table table-striped table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ categorie.naam }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}"><button class="btn btn-primary">Terug naar lijst</button></a>
<br><br>
    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}"><button class="btn btn-success">Bewerken</button></a>
<br><br>
    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
