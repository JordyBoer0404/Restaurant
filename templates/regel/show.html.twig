{% extends 'base.html.twig' %}

{% block title %}Regel{% endblock %}

{% block body %}
    <h1>Regel</h1>

    <table class="table table-striped table-responsive">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ regel.id }}</td>
            </tr>
            <tr>
                <th>Aantal</th>
                <td>{{ regel.aantal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('regel_index') }}"><button class="btn btn-primary">Terug naar lijst</button></a>
<br><br>
    <a href="{{ path('regel_edit', {'id': regel.id}) }}"><button class="btn btn-success">Bewerken</button></a>
<br><br>
    {{ include('regel/_delete_form.html.twig') }}
{% endblock %}
